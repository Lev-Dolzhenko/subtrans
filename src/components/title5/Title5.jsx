function Title5({lineHeight, children}) {
    return (
        <div>
            <h5 className={`text-colorTitle font-bold font-montserrat text-xl	 ${lineHeight}`}>{children}</h5>
        </div>
    );
}

export default Title5;